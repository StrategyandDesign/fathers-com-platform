/* Fathers.com runtime config. This file is gitignored. Paste keys here.
   Leave empty for demo mode (no accounts, local-only progress). */
window.FC_CONFIG = {
  SUPABASE_URL: "",            // https://YOUR-PROJECT.supabase.co
  SUPABASE_ANON_KEY: "",       // anon public key only, never the service key
  STRIPE_PAYMENT_LINK: ""      // optional: Stripe Payment Link URL for the $120 annual
};
